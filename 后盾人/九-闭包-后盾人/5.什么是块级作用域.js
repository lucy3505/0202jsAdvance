//块作用域
{
    let a = 1//const也一样，只在块级里面有作用
    console.log(a)
}

// console.log(a)//报错，无法使用
{ 
    let a =1 //两个不同的环境
    var a = 2//不能再块作用域中声明同名字的变量
    // !函数内部用var声明得只能在函数内部使用
}

{
    var a =1//会声明到全局中，全局可以访问
}